export type User = {
  id: string;
  name: string;
  email: string;
  role: string;
  phone: string;
};

export type Address = {
  id: string;
  street: string;
  number: string;
  city: string;
  neighborhood: string;
  state: string;
  cep: string;
};
